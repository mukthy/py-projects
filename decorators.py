# it usually have @ symbol at the start. It is possible because these functions acts as variables.

# A function which accepts another function is a higher order function.

def great(func):
    func()
